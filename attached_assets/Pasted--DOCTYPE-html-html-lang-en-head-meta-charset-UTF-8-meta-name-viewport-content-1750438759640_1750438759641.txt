<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guest Manager</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #f5f5f7;
            color: #1d1d1f;
            line-height: 1.6;
        }

        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 20px 16px;
            text-align: center;
            box-shadow: 0 2px 20px rgba(0,0,0,0.1);
        }

        .header h1 {
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 8px;
        }

        .header p {
            opacity: 0.9;
            font-size: 14px;
        }

        .container {
            padding: 20px 16px;
            max-width: 100%;
        }

        .search-section {
            background: white;
            border-radius: 16px;
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
        }

        .search-box {
            position: relative;
            margin-bottom: 20px;
        }

        .search-box input {
            width: 100%;
            padding: 16px 20px 16px 50px;
            border: 2px solid #e5e5e7;
            border-radius: 12px;
            font-size: 16px;
            background: #f8f8fa;
            transition: all 0.3s ease;
        }

        .search-box input:focus {
            outline: none;
            border-color: #667eea;
            background: white;
            box-shadow: 0 0 0 4px rgba(102, 126, 234, 0.1);
        }

        .search-icon {
            position: absolute;
            left: 18px;
            top: 50%;
            transform: translateY(-50%);
            color: #86868b;
            font-size: 18px;
        }

        .filter-section {
            display: grid;
            gap: 16px;
        }

        .filter-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .filter-label {
            font-weight: 600;
            color: #1d1d1f;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .filter-select {
            width: 100%;
            padding: 14px 16px;
            border: 2px solid #e5e5e7;
            border-radius: 10px;
            font-size: 16px;
            background: white;
            color: #1d1d1f;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .filter-select:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 4px rgba(102, 126, 234, 0.1);
        }

        .guest-list-section {
            background: white;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
        }

        .section-header {
            padding: 20px;
            background: linear-gradient(90deg, #f8f8fa 0%, #e5e5e7 100%);
            border-bottom: 1px solid #e5e5e7;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .section-title {
            font-size: 18px;
            font-weight: 600;
            color: #1d1d1f;
        }

        .guest-count {
            background: #667eea;
            color: white;
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
        }

        .select-all-btn {
            background: none;
            border: 2px solid #667eea;
            color: #667eea;
            padding: 8px 16px;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .select-all-btn:hover {
            background: #667eea;
            color: white;
        }

        .guest-item {
            padding: 20px;
            border-bottom: 1px solid #f0f0f0;
            display: flex;
            align-items: center;
            gap: 16px;
            transition: background-color 0.3s ease;
        }

        .guest-item:hover {
            background: #f8f8fa;
        }

        .guest-item:last-child {
            border-bottom: none;
        }

        .guest-checkbox {
            width: 20px;
            height: 20px;
            border: 2px solid #e5e5e7;
            border-radius: 4px;
            cursor: pointer;
            position: relative;
            transition: all 0.3s ease;
        }

        .guest-checkbox.checked {
            background: #667eea;
            border-color: #667eea;
        }

        .guest-checkbox.checked::after {
            content: '✓';
            position: absolute;
            color: white;
            font-size: 14px;
            top: -2px;
            left: 2px;
        }

        .guest-info {
            flex: 1;
        }

        .guest-name {
            font-weight: 600;
            color: #1d1d1f;
            margin-bottom: 4px;
        }

        .guest-details {
            font-size: 14px;
            color: #86868b;
        }

        .guest-status {
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .status-confirmed {
            background: #d4edda;
            color: #155724;
        }

        .status-pending {
            background: #fff3cd;
            color: #856404;
        }

        .status-cancelled {
            background: #f8d7da;
            color: #721c24;
        }

        .guest-actions {
            display: flex;
            gap: 8px;
        }

        .action-btn {
            width: 36px;
            height: 36px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
            transition: all 0.3s ease;
        }

        .btn-info {
            background: #e3f2fd;
            color: #1976d2;
        }

        .btn-info:hover {
            background: #bbdefb;
        }

        .btn-edit {
            background: #fff3e0;
            color: #f57c00;
        }

        .btn-edit:hover {
            background: #ffe0b2;
        }

        .btn-delete {
            background: #ffebee;
            color: #d32f2f;
        }

        .btn-delete:hover {
            background: #ffcdd2;
        }

        .empty-state {
            padding: 40px 20px;
            text-align: center;
            color: #86868b;
        }

        .empty-state-icon {
            font-size: 48px;
            margin-bottom: 16px;
            opacity: 0.5;
        }

        @media (max-width: 480px) {
            .header {
                padding: 16px;
            }
            
            .header h1 {
                font-size: 20px;
            }
            
            .container {
                padding: 16px 12px;
            }
            
            .guest-item {
                padding: 16px;
                flex-wrap: wrap;
            }
            
            .guest-actions {
                width: 100%;
                justify-content: center;
                margin-top: 12px;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Guest Manager</h1>
        <p>Manage your event guests efficiently</p>
    </div>

    <div class="container">
        <!-- Search and Filter Section -->
        <div class="search-section">
            <div class="search-box">
                <span class="search-icon">🔍</span>
                <input type="text" id="searchInput" placeholder="Search guests...">
            </div>
            
            <div class="filter-section">
                <div class="filter-group">
                    <label class="filter-label">Filter by Status</label>
                    <select class="filter-select" id="statusFilter">
                        <option value="all">All Status</option>
                        <option value="confirmed">Confirmed</option>
                        <option value="pending">Pending</option>
                        <option value="cancelled">Cancelled</option>
                    </select>
                </div>
                
                <div class="filter-group">
                    <label class="filter-label">Comments</label>
                    <select class="filter-select" id="commentFilter">
                        <option value="all">All Comments</option>
                        <option value="with">With Comments</option>
                        <option value="without">Without Comments</option>
                    </select>
                </div>
            </div>
        </div>

        <!-- Guest List Section -->
        <div class="guest-list-section">
            <div class="section-header">
                <div>
                    <div class="section-title">Guest List</div>
                    <span class="guest-count" id="guestCount">3 guests</span>
                </div>
                <button class="select-all-btn" onclick="toggleSelectAll()">Select All</button>
            </div>

            <div id="guestList">
                <div class="guest-item">
                    <div class="guest-checkbox" onclick="toggleCheckbox(this)"></div>
                    <div class="guest-info">
                        <div class="guest-name">Functionality Test</div>
                        <div class="guest-details">API test guest • No comments</div>
                    </div>
                    <div class="guest-status status-confirmed">Confirmed</div>
                    <div class="guest-actions">
                        <button class="action-btn btn-info" title="View Details">ℹ️</button>
                        <button class="action-btn btn-edit" title="Edit">✏️</button>
                        <button class="action-btn btn-delete" title="Delete">🗑️</button>
                    </div>
                </div>

                <div class="guest-item">
                    <div class="guest-checkbox" onclick="toggleCheckbox(this)"></div>
                    <div class="guest-info">
                        <div class="guest-name">John Smith</div>
                        <div class="guest-details">john@example.com • Has comments</div>
                    </div>
                    <div class="guest-status status-pending">Pending</div>
                    <div class="guest-actions">
                        <button class="action-btn btn-info" title="View Details">ℹ️</button>
                        <button class="action-btn btn-edit" title="Edit">✏️</button>
                        <button class="action-btn btn-delete" title="Delete">🗑️</button>
                    </div>
                </div>

                <div class="guest-item">
                    <div class="guest-checkbox" onclick="toggleCheckbox(this)"></div>
                    <div class="guest-info">
                        <div class="guest-name">Sarah Johnson</div>
                        <div class="guest-details">sarah@example.com • Dietary restrictions noted</div>
                    </div>
                    <div class="guest-status status-confirmed">Confirmed</div>
                    <div class="guest-actions">
                        <button class="action-btn btn-info" title="View Details">ℹ️</button>
                        <button class="action-btn btn-edit" title="Edit">✏️</button>
                        <button class="action-btn btn-delete" title="Delete">🗑️</button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        let allSelected = false;

        function toggleCheckbox(checkbox) {
            checkbox.classList.toggle('checked');
            updateGuestCount();
        }

        function toggleSelectAll() {
            allSelected = !allSelected;
            const checkboxes = document.querySelectorAll('.guest-checkbox');
            const button = document.querySelector('.select-all-btn');
            
            checkboxes.forEach(checkbox => {
                if (allSelected) {
                    checkbox.classList.add('checked');
                } else {
                    checkbox.classList.remove('checked');
                }
            });
            
            button.textContent = allSelected ? 'Deselect All' : 'Select All';
            updateGuestCount();
        }

        function updateGuestCount() {
            const totalGuests = document.querySelectorAll('.guest-item').length;
            const selectedGuests = document.querySelectorAll('.guest-checkbox.checked').length;
            const countElement = document.getElementById('guestCount');
            
            if (selectedGuests > 0) {
                countElement.textContent = `${selectedGuests} of ${totalGuests} selected`;
                countElement.style.background = '#f57c00';
            } else {
                countElement.textContent = `${totalGuests} guests`;
                countElement.style.background = '#667eea';
            }
        }

        // Search functionality
        document.getElementById('searchInput').addEventListener('input', function(e) {
            const searchTerm = e.target.value.toLowerCase();
            const guestItems = document.querySelectorAll('.guest-item');
            
            guestItems.forEach(item => {
                const guestName = item.querySelector('.guest-name').textContent.toLowerCase();
                const guestDetails = item.querySelector('.guest-details').textContent.toLowerCase();
                
                if (guestName.includes(searchTerm) || guestDetails.includes(searchTerm)) {
                    item.style.display = 'flex';
                } else {
                    item.style.display = 'none';
                }
            });
        });

        // Filter functionality
        document.getElementById('statusFilter').addEventListener('change', function(e) {
            const filterValue = e.target.value;
            const guestItems = document.querySelectorAll('.guest-item');
            
            guestItems.forEach(item => {
                const status = item.querySelector('.guest-status').textContent.toLowerCase();
                
                if (filterValue === 'all' || status.includes(filterValue)) {
                    item.style.display = 'flex';
                } else {
                    item.style.display = 'none';
                }
            });
        });

        document.getElementById('commentFilter').addEventListener('change', function(e) {
            const filterValue = e.target.value;
            const guestItems = document.querySelectorAll('.guest-item');
            
            guestItems.forEach(item => {
                const details = item.querySelector('.guest-details').textContent.toLowerCase();
                const hasComments = !details.includes('no comments');
                
                if (filterValue === 'all' || 
                    (filterValue === 'with' && hasComments) || 
                    (filterValue === 'without' && !hasComments)) {
                    item.style.display = 'flex';
                } else {
                    item.style.display = 'none';
                }
            });
        });
    </script>
</body>
</html>